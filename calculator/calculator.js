add = (a,b) => (a + b);

subtract = (a,b) => (a - b);

sum = ([]) => [].reduce((a,b) => (a + b), 0);

function multiply () {
	
}

function power() {
	
}

function factorial() {
	
}

module.exports = {
	add,
	subtract,
	sum,
	multiply,
    power,
	factorial
}